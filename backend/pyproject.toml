[project]
name = "polls"
version = "0.1.0"
description = "Polls"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.6",
    "djangorestframework>=3.16.1",
]

[dependency-groups]
dev = [
    "ruff>=0.13.0",
]
